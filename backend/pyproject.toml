[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "the-gazer"
version = "0.1.0"
description = "FastAPI backend skeleton for The Gazer"
requires-python = ">=3.11, <3.14"
dependencies = [
  "agentmail>=0.2.22",
  "anthropic>=0.52.0",
  "browser-use>=0.12.0",
  "browser-use-sdk>=2.0.15",
  "exa-py>=2.6.1",
  "fastapi>=0.116.0",
  "google-generativeai>=0.8.5",
  "google-genai>=1.60.0",
  "httpx>=0.28.1",
  "langchain-openai>=0.3.0",
  "loguru>=0.7.3",
  "numpy>=2.4.2",
  "opencv-python>=4.10.0",
  "PicImageSearch>=3.12.11",
  "pillow>=12.1.0",
  "pyasn1>=0.6.3",
  "PyJWT>=2.12.0",
  "pydantic-settings>=2.10.1",
  "python-multipart>=0.0.27",
  "sherlock-project @ https://github.com/sherlock-project/sherlock/archive/refs/heads/master.zip",
  "sse-starlette>=1.8.0",
  "twscrape>=0.17.0",
  "uvicorn>=0.35.0",
]

[project.optional-dependencies]
dev = [
  "httpx>=0.28.1",
  "pytest>=9.0.3",
  "pytest-cov>=6.0",
  "ruff>=0.12.11",
]

[tool.setuptools]
py-modules = ["config", "env_check", "main", "pipeline", "schemas", "tracing"]
packages = ["agents", "capture", "db", "enrichment", "identification", "intel", "synthesis"]

[tool.pytest.ini_options]
addopts = "--cov=. --cov-report=term-missing --cov-config=pyproject.toml"
asyncio_mode = "auto"

[tool.coverage.run]
source = ["."]
omit = [
  "tests/*",
  ".venv/*",
  "__pycache__/*",
]

[tool.coverage.report]
fail_under = 46
show_missing = true
exclude_lines = [
  "pragma: no cover",
  "if __name__ == .__main__.",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["B", "E", "F", "I", "UP"]

[tool.uv]
override-dependencies = [
    "aiohttp>=3.13.4",
    "langchain-openai>=1.1.14",
    "lxml>=6.1.0",
    "openai>=2.26.0",
    "pillow>=12.2.0",
    "pygments>=2.20.0",
    "pypdf>=6.10.2",
    "python-dotenv>=1.2.2",
    "requests>=2.33.0",
]

[dependency-groups]
dev = [
    "pytest-asyncio>=1.3.0",
]
